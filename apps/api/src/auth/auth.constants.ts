/** Name of the httpOnly cookie holding the refresh token. */
export const REFRESH_COOKIE = "refresh_token";

/** Path the refresh cookie is scoped to (so it is only sent to /auth/*). */
export const REFRESH_COOKIE_PATH = "/auth";
